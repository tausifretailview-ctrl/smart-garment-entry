

## Migration: Clean Up Duplicate Indexes

Run the user's provided SQL as a database migration to drop and recreate duplicate indexes on `audit_logs`, `product_variants`, and `sales` tables.

### Action
- Create one migration with the exact SQL provided — no modifications needed.

