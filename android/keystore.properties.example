# Copy to keystore.properties (never commit). Keystore file lives OUTSIDE the repo.
storeFile=C:/path/to/ezzyerp-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=ezzyerp
keyPassword=YOUR_KEY_PASSWORD
